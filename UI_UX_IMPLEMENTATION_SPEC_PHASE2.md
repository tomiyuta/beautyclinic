# UI/UX改善 Phase 2 実装詳細要件定義書

## 1. 概要
本ドキュメントは、`beauty_project` のUI/UX改善 Phase 2における実装要件を定義するものである。
Phase 1で整備された基盤（リファクタリング、基本デザイン）をベースに、ユーザー体験を大幅に向上させるための機能実装を行う。

**目的:**
- 複雑な操作の簡素化（ウィザード化）
- 意思決定のサポート（入力支援）
- アプリケーションの洗練（マイクロインタラクション）

---

## 2. 対象範囲 (Scope) - MECE分類

本要件定義書は、アプリケーションを以下の4つの象限に分類して改善項目を定義する。

| | **機能要件 (Logic/Functionality)** | **非機能要件 (UI/UX/Visual)** |
| :--- | :--- | :--- |
| **特定機能 (Feature Specific)** | **A. コンテンツ生成ウィザード**<br>・ステップ分割ロジック<br>・状態管理の永続化 | **B. 商品管理の入力支援**<br>・利益シミュレーション表示<br>・動的バリデーションUI |
| **全体共通 (Global)** | **C. 入力コンポーネント拡張**<br>・共通ウィザード基盤<br>・数値入力フォーマッタ | **D. ナビゲーション強化**<br>・アクティブ状態アニメーション<br>・インタラクションフィードバック |

---

## 3. 詳細要件

### A. コンテンツ生成ウィザード (Content Generation Wizard)

現状の一画面集約型フォームを、認知的負荷を下げるためにステップ実行型（ウィザード）に変更する。

#### A-1. ステップ構成
以下の4ステップに分割する。

1.  **Step 1: 基本設定 (Basics)**
    *   **入力項目**: コンテンツカテゴリー、コンテンツタイプ
    *   **UI**: 大きなアイコン付きの選択カード（クリックで次へ）
    *   **条件**: 選択必須
2.  **Step 2: キャンペーン情報 (Campaign Info)**
    *   **入力項目**: キャンペーン名、説明、ターゲット層、プロモーション詳細
    *   **機能**: 過去のキャンペーンからの複製機能（オプション）
3.  **Step 3: 詳細設定 (Details)**
    *   **動的表示**: Step 1で選択したタイプに応じたフォームを表示
        *   テキストの場合: トーン、キーワード、文字数
        *   画像の場合: スタイル、色味、アスペクト比
        *   動画の場合（将来用）: 尺、スタイル
4.  **Step 4: プレビューと生成 (Preview & Generate)**
    *   **表示**: 設定内容のサマリ表示
    *   **機能**: 生成実行ボタン、戻るボタン

#### A-2. UIコンポーネント要件
*   **プログレスバー**: 現在のステップ位置と全体の進捗を上部に表示する。
*   **ナビゲーション**: 「次へ」「戻る」ボタンを下部に配置（Step 1は選択で自動遷移）。
*   **状態保持**: ステップを行き来しても入力内容は保持されること。

---

### B. 商品管理の入力支援 (Product Management Assistance)

商品登録時の価格設定をサポートするため、数値的な根拠をリアルタイムで提示する。

#### B-1. 利益シミュレーション機能
*   **トリガー**: 「原価 (`costPrice`)」または「販売価格 (`sellingPrice`)」の入力変更時。
*   **計算ロジック**:
    *   `利益額 = 販売価格 - 原価`
    *   `利益率 = (利益額 / 販売価格) * 100` (小数点第1位まで表示)
*   **表示UI**:
    *   入力フィールドの近傍（右側または下部）にインジケーターを表示。
    *   **色分け**:
        *   利益率 < 0% (赤字): 赤色アイコン + 警告メッセージ
        *   0% <= 利益率 < 20%: 黄色アイコン
        *   20% <= 利益率: 青/緑色アイコン（適正）

#### B-2. リアルタイムフォーマット
*   入力時に3桁区切り（カンマ）を自動挿入するが、内部値は数値として管理する。

---

### C. 入力コンポーネント拡張 (Component Extension)

#### C-1. 汎用ウィザードコンポーネント (`WizardLayout`)
コンテンツ生成以外でも使用可能な汎用コンポーネントとして実装する。

```typescript
interface WizardStep {
  id: string;
  title: string;
  component: React.ReactNode;
  validationSchema?: ZodSchema; // 各ステップのバリデーション
}
```

---

### D. ナビゲーション強化 (Navigation Enhancement)

アプリ全体の質感（Quality of Life）を向上させるためのマイクロインタラクション。

#### D-1. アクティブインジケーター (Magic Line)
*   **対象**: デスクトップナビゲーション、タブ切り替え
*   **動作**:
    *   現在選択されている項目の下に、アニメーションする下線（または背景）を表示。
    *   項目切り替え時、線が「スライド」して移動する表現（Framer Motionの `layoutId` を使用）。
*   **目的**: 現在地の明確化と、操作の楽しさの提供。

#### D-2. ボタンインタラクション
*   **Hover**: わずかなスケールアップ (scale: 1.02) とシャドウの増加。
*   **Tap/Click**: わずかなスケールダウン (scale: 0.98) で「押した感」を演出。

---

## 4. 実装ステップと優先順位

実装は以下の順序で行うことを推奨する。

1.  **共通コンポーネント実装**
    *   `src/components/ui/Wizard` (新規作成)
    *   `src/components/ui/PriceSimulation` (新規作成)
2.  **コンテンツ生成ページ改修 (`src/features/content/content-generation.tsx`)**
    *   既存のフォームを分割し、`Wizard` コンポーネントに組み込む。
3.  **商品管理ページ改修 (`src/features/products/product-management.tsx`)**
    *   `AnimatedTextField` と連動する `PriceSimulation` を配置。
4.  **ナビゲーション改修 (`src/components/Navigation.tsx`)**
    *   `framer-motion` を用いたアニメーションの追加。

---

## 5. 技術スタック・制約

*   **Framework**: Next.js (App Router)
*   **Styling**: Tailwind CSS / Inline Styles (現状維持、徐々にTailwindへ)
*   **Animation**: Framer Motion (必須)
*   **State Management**: React Hook Form (推奨) または 既存の `useState` の組み合わせ
*   **Icon**: 既存のアイコンセットを使用

---

## 6. 完了条件 (Definition of Done)

1.  **コンテンツ生成**: カテゴリ選択から生成開始まで、ページ遷移なしでスムーズに完了できること。
2.  **商品管理**: 原価1000円、売価2000円と入力した際、「利益: 1,000円 (50.0%)」と表示されること。
3.  **ナビゲーション**: メニュー切り替え時にスムーズなアニメーションが確認できること。
4.  **エラーなし**: console errorが発生していないこと。
5.  **ビルド成功**: `npm run build` が正常に完了すること。

