# Acontext統合PoC 要件定義書

## 文書情報

- **作成日**: 2025-01-XX
- **バージョン**: 1.0
- **プロジェクト名**: beauty project × Acontext統合PoC
- **目的**: Acontextシステムの統合可能性と価値を実証するための概念実証（Proof of Concept）

---

## 1. エグゼクティブサマリー

### 1.1. PoCの目的

本PoCは、Acontextシステムをbeauty projectに統合することで、以下の価値を実証することを目的とします：

1. **会話コンテキストの永続化**: マルチターン対話の実現とユーザー体験の向上
2. **タスクの自動追跡**: 戦略分析プロセスの可視化と進捗管理の自動化
3. **スキル学習の基盤**: 過去の成功パターンの蓄積と再利用の可能性

### 1.2. PoCの範囲

**対象機能:**
- 戦略分析機能（`/strategy-analysis`）の「総合分析」のみ
- セッション管理とメッセージ保存
- タスクの自動抽出と進捗表示

**対象外:**
- 他の戦略分析機能（価格設定提案、キャンペーン案生成、新施術導入提案）
- コンテンツ生成機能
- 市場調査・SNS調査機能
- スキル学習機能（フェーズ2以降）

### 1.3. 成功基準

PoCは以下の基準を満たした場合に成功とみなします：

1. **技術的成功**
   - Acontextサーバーが正常に起動し、beauty projectから接続可能
   - セッションの作成・メッセージの保存が正常に動作
   - タスクの自動抽出が正常に動作

2. **機能的な価値**
   - 戦略分析の実行時にセッションが作成され、会話履歴が保存される
   - タスクが自動的に抽出され、進捗が可視化される
   - Acontextダッシュボードでセッション・タスクを確認できる

3. **パフォーマンス**
   - 既存の戦略分析機能の応答時間に影響を与えない（±10%以内）
   - Acontext統合による追加のレイテンシが500ms以内

4. **運用性**
   - エラーハンドリングが適切に実装されている
   - Acontextサーバーが停止しても既存機能に影響しない（グレースフルデグラデーション）

---

## 2. 現状分析

### 2.1. 既存システムの動作

**戦略分析（総合分析）の現状:**

1. ユーザーが以下を入力:
   - 場所（必須）
   - 商品IDs（オプション）
   - 市場データを含める（デフォルト: true）
   - SNSデータを含める（デフォルト: true）

2. システムが以下を実行:
   - ユーザー設定からAIプロバイダーを取得（Claude/ChatGPT/Gemini）
   - 商品データを取得
   - 市場調査結果を取得（オプション）
   - SNS調査結果を取得（オプション）
   - AIエージェントにプロンプトを送信
   - 結果を`StrategyRecommendation`テーブルに保存
   - 結果をユーザーに返却

3. **課題:**
   - 会話履歴が保存されない（各リクエストが独立）
   - タスクの進捗が追跡されない
   - 過去の分析結果を参照できない
   - ユーザーとの対話履歴がない

### 2.2. Acontext統合後の期待される動作

1. ユーザーが戦略分析を実行
2. システムが以下を実行:
   - Acontextセッションを作成（ユーザー専用Spaceに接続）
   - ユーザーのリクエストをセッションに保存
   - 既存のロジックでAI分析を実行
   - AI応答をセッションに保存
   - タスクを自動抽出（バックグラウンド）
   - 結果を既存テーブルに保存（互換性維持）
3. ユーザーが以下を確認可能:
   - 分析結果（既存UI）
   - セッションID（新規）
   - タスク進捗（新規UI、オプション）
   - Acontextダッシュボードで詳細確認

---

## 3. 機能要件

### 3.1. セッション管理機能

#### 3.1.1. セッションの自動作成

**要件:**
- 戦略分析（総合分析）実行時に、Acontextセッションを自動作成
- セッションはユーザー専用のSpaceに接続
- セッションIDをレスポンスに含める（オプション）

**入力:**
- `userId` (number)

**出力:**
- `sessionId` (string) - AcontextセッションID

**ビジネスルール:**
- ユーザーごとにSpaceを作成（初回のみ）
- Space名: `beauty-clinic-user-{userId}`
- セッション作成に失敗した場合、既存機能は継続（グレースフルデグラデーション）

**エラーハンドリング:**
- Acontextサーバーが利用不可の場合、セッション作成をスキップ
- エラーログを記録するが、ユーザーには表示しない

#### 3.1.2. メッセージの保存

**要件:**
- ユーザーのリクエスト（場所、商品IDsなど）をセッションに保存
- AI応答（分析結果）をセッションに保存
- メッセージフォーマット: OpenAI形式（`role`, `content`）

**入力:**
- `sessionId` (string)
- `role` ("user" | "assistant")
- `content` (string)

**出力:**
- 成功/失敗のステータス

**ビジネスルール:**
- メッセージ保存に失敗しても、既存機能は継続
- 非同期で保存（ユーザーの応答時間に影響しない）

### 3.2. タスク自動抽出機能

#### 3.2.1. タスクの自動抽出

**要件:**
- AcontextのTask Agentがバックグラウンドでタスクを自動抽出
- タスクには以下が含まれる:
  - タスクの説明
  - ステータス（pending, success, failed）
  - 進捗更新（オプション）
  - ユーザーの好み（オプション）

**動作:**
- メッセージ送信後、バックグラウンドでタスク抽出が実行される
- 抽出完了まで10-30秒かかる場合がある
- 即座に結果を返す必要はない（非同期処理）

#### 3.2.2. タスク進捗の表示（オプション）

**要件:**
- フロントエンドでタスク進捗を表示するUI（オプション機能）
- セッションIDからタスクを取得
- タスクのステータスと進捗を表示

**UI要件:**
- タスク一覧の表示
- 各タスクのステータス表示
- 進捗更新の表示（オプション）

**優先度:** 低（PoCでは実装しない場合もある）

### 3.3. 既存機能との統合

#### 3.3.1. 既存APIの拡張

**要件:**
- 既存の`strategy.analyzeMarketPosition`エンドポイントを拡張
- 既存のレスポンス形式を維持（後方互換性）
- オプションで`sessionId`を追加

**レスポンス形式:**
```typescript
{
  // 既存のフィールド（変更なし）
  marketPosition: string,
  strengths: string[],
  weaknesses: string[],
  opportunities: string[],
  threats: string[],
  recommendations: string[],
  
  // 新規フィールド（オプション）
  sessionId?: string
}
```

#### 3.3.2. エラーハンドリング

**要件:**
- Acontext関連のエラーは既存機能に影響しない
- エラーログを記録
- ユーザーには既存のエラーメッセージを表示

**エラーケース:**
1. Acontextサーバーが起動していない
2. ネットワークエラー
3. API認証エラー
4. タイムアウト

**対応:**
- すべてのエラーケースで、既存機能は継続
- エラーログに記録（開発環境ではコンソールにも出力）

---

## 4. 非機能要件

### 4.1. パフォーマンス要件

**応答時間:**
- 既存の戦略分析機能の応答時間に影響を与えない
- Acontext統合による追加レイテンシ: 500ms以内
- メッセージ保存は非同期で実行（ユーザーの応答時間に影響しない）

**スループット:**
- 既存のスループットを維持
- Acontextサーバーへのリクエストは非同期で実行

### 4.2. 可用性要件

**グレースフルデグラデーション:**
- Acontextサーバーが利用不可の場合、既存機能は継続
- エラーメッセージを表示しない（ユーザー体験への影響を最小化）

**監視:**
- Acontextサーバーのヘルスチェック（オプション）
- エラーログの記録

### 4.3. セキュリティ要件

**認証:**
- Acontext APIキーを環境変数で管理
- APIキーをコードにハードコードしない

**データプライバシー:**
- セッションデータはユーザーごとに分離
- Spaceはユーザー単位で作成

### 4.4. 運用要件

**ログ:**
- Acontext関連の操作をログに記録
- エラーは既存のエラーログシステムに記録

**デバッグ:**
- 開発環境では詳細なログを出力
- 本番環境ではエラーログのみ

---

## 5. 技術要件

### 5.1. システム構成

**追加コンポーネント:**
- Acontextサーバー（Docker Composeで起動）
- Acontext TypeScript SDK (`@acontext/acontext`)

**既存コンポーネント:**
- beauty project（Next.js + tRPC + Prisma + MySQL）
- 変更なし

### 5.2. データベース

**既存データベース:**
- MySQL（変更なし）

**Acontextデータベース:**
- PostgreSQL（Acontextサーバー内で管理）
- beauty projectからは直接アクセスしない

### 5.3. API統合

**Acontext API:**
- Base URL: `http://localhost:8029/api/v1` (開発環境)
- 認証: Bearer Token
- エンドポイント:
  - `POST /sessions` - セッション作成
  - `POST /sessions/{sessionId}/messages` - メッセージ送信
  - `GET /sessions/{sessionId}/tasks` - タスク取得（オプション）

### 5.4. 依存関係

**追加パッケージ:**
```json
{
  "dependencies": {
    "@acontext/acontext": "^0.0.4"
  }
}
```

**環境変数:**
```env
ACONTEXT_BASE_URL=http://localhost:8029/api/v1
ACONTEXT_API_KEY=sk-ac-your-root-api-bearer-token
```

### 5.5. 開発環境

**必要なツール:**
- Docker & Docker Compose（Acontextサーバー起動用）
- Node.js 18以上（既存）
- MySQL（既存）

---

## 6. 実装スコープ

### 6.1. PoCで実装する機能

1. **Acontextクライアントサービスの作成**
   - `src/server/services/acontext-client.ts`
   - セッション作成、メッセージ送信のラッパー関数

2. **戦略分析ルーターの拡張**
   - `src/server/api/routers/strategy.ts`を修正
   - セッション作成とメッセージ保存を追加

3. **エラーハンドリングの実装**
   - グレースフルデグラデーション
   - エラーログの記録

4. **環境変数の設定**
   - `.env`ファイルへの追加
   - ドキュメントの作成

### 6.2. PoCで実装しない機能

1. **フロントエンドUIの変更**
   - タスク進捗表示（フェーズ2以降）
   - セッション履歴表示（フェーズ2以降）

2. **スキル学習機能**
   - Spaceへのスキル保存（フェーズ2以降）
   - スキル検索（フェーズ2以降）

3. **他の機能への統合**
   - コンテンツ生成（フェーズ2以降）
   - 市場調査・SNS調査（フェーズ2以降）

4. **高度な機能**
   - コンテキスト圧縮（フェーズ3以降）
   - ディスクへのオフロード（フェーズ3以降）

### 6.3. テストスコープ

**単体テスト:**
- Acontextクライアントサービスのテスト
- エラーハンドリングのテスト

**統合テスト:**
- 戦略分析実行時のセッション作成
- メッセージ保存の動作確認

**手動テスト:**
- Acontextダッシュボードでの確認
- エラーケースの確認

---

## 7. データモデル

### 7.1. 既存データモデル（変更なし）

```prisma
model StrategyRecommendation {
  id                      Int
  userId                  Int
  analysisDate            DateTime
  priceRecommendations    String?
  campaignProposals       String?
  newTreatmentSuggestions String?
  marketingStrategy       String?
  userFeedback            String?
  implementationStatus    ImplementationStatus
  createdAt               DateTime
  updatedAt               DateTime
}
```

### 7.2. Acontextデータモデル（参照のみ）

Acontextは独自のデータベース（PostgreSQL）で管理:
- `sessions` - セッションテーブル
- `messages` - メッセージテーブル
- `tasks` - タスクテーブル
- `spaces` - Spaceテーブル

beauty projectからは直接アクセスせず、API経由でアクセス。

### 7.3. データ連携

**セッションIDの保存（オプション）:**
- `StrategyRecommendation`テーブルに`sessionId`カラムを追加（オプション）
- または、別テーブルで管理

**推奨:**
- PoCでは、レスポンスに`sessionId`を含めるのみ
- データベースへの保存はフェーズ2以降で検討

---

## 8. UI/UX要件

### 8.1. 既存UI（変更なし）

**戦略分析ページ:**
- 入力フォーム（変更なし）
- 結果表示（変更なし）

### 8.2. 新規UI（オプション）

**タスク進捗表示（フェーズ2以降）:**
- セッションIDからタスクを取得
- タスク一覧を表示
- 進捗をリアルタイムで更新

**PoCでは実装しない**

### 8.3. Acontextダッシュボード

**アクセス:**
- URL: `http://localhost:3000/` (Acontextダッシュボード)
- セッション、メッセージ、タスクを確認可能

**PoCでの確認項目:**
- セッションが作成されているか
- メッセージが保存されているか
- タスクが抽出されているか

---

## 9. セキュリティ要件

### 9.1. API認証

**Acontext APIキー:**
- 環境変数で管理
- コードにハードコードしない
- バージョン管理に含めない（`.gitignore`に追加）

### 9.2. データプライバシー

**ユーザー分離:**
- セッションはユーザーごとに分離
- Spaceはユーザー単位で作成
- 他のユーザーのデータにアクセスできない

### 9.3. エラーメッセージ

**情報漏洩防止:**
- エラーメッセージに機密情報を含めない
- スタックトレースは開発環境のみ

---

## 10. テスト要件

### 10.1. 単体テスト

**対象:**
- `acontext-client.ts`の各関数
- エラーハンドリング

**テストケース:**
1. セッション作成の成功
2. セッション作成の失敗（サーバー停止）
3. メッセージ送信の成功
4. メッセージ送信の失敗

### 10.2. 統合テスト

**対象:**
- 戦略分析実行時のAcontext統合

**テストケース:**
1. 正常系: セッション作成 → メッセージ保存 → タスク抽出
2. 異常系: Acontextサーバー停止時の動作
3. 異常系: ネットワークエラー時の動作

### 10.3. 手動テスト

**確認項目:**
1. Acontextダッシュボードでセッションを確認
2. メッセージが正しく保存されているか
3. タスクが自動抽出されているか
4. エラーケースの動作確認

---

## 11. デプロイ要件

### 11.1. 開発環境

**セットアップ手順:**
1. Acontext CLIのインストール
2. Acontextサーバーの起動（Docker Compose）
3. 環境変数の設定
4. 依存関係のインストール

### 11.2. 本番環境（PoCでは対象外）

**考慮事項:**
- Acontextサーバーのデプロイ方法
- 環境変数の管理
- モニタリングとログ

**PoCでは開発環境のみ**

---

## 12. スケジュール

### 12.1. PoC実装スケジュール

**フェーズ1: セットアップ（1-2日）**
- Acontextサーバーのセットアップ
- SDKのインストール
- 環境変数の設定

**フェーズ2: 実装（3-5日）**
- Acontextクライアントサービスの作成
- 戦略分析ルーターの拡張
- エラーハンドリングの実装

**フェーズ3: テスト（2-3日）**
- 単体テスト
- 統合テスト
- 手動テスト

**フェーズ4: ドキュメント（1日）**
- 実装ドキュメントの作成
- 運用マニュアルの作成

**合計: 7-11日（約2週間）**

### 12.2. マイルストーン

1. **M1: セットアップ完了**（2日目）
   - Acontextサーバーが起動
   - SDKがインストール済み

2. **M2: 基本機能実装完了**（5日目）
   - セッション作成が動作
   - メッセージ保存が動作

3. **M3: 統合完了**（7日目）
   - 戦略分析実行時にセッションが作成される
   - メッセージが保存される

4. **M4: テスト完了**（10日目）
   - すべてのテストがパス
   - 手動テストが完了

5. **M5: PoC完了**（11日目）
   - ドキュメントが完成
   - デモが可能

---

## 13. リスクと対策

### 13.1. 技術的リスク

**リスク1: Acontextサーバーの安定性**
- **影響**: 高
- **確率**: 中
- **対策**: グレースフルデグラデーションを実装

**リスク2: パフォーマンスへの影響**
- **影響**: 中
- **確率**: 中
- **対策**: 非同期処理で実装、タイムアウト設定

**リスク3: データベースの違い（MySQL vs PostgreSQL）**
- **影響**: 低
- **確率**: 低
- **対策**: API経由でアクセス（直接アクセスしない）

### 13.2. 運用リスク

**リスク4: 追加インフラの運用負荷**
- **影響**: 中
- **確率**: 中
- **対策**: Docker Composeで簡単に起動・停止可能

**リスク5: 学習コスト**
- **影響**: 低
- **確率**: 高
- **対策**: ドキュメントの充実、段階的な実装

### 13.3. ビジネスリスク

**リスク6: PoCの価値が実証できない**
- **影響**: 高
- **確率**: 低
- **対策**: 明確な成功基準を設定、段階的な評価

---

## 14. 成功基準の詳細

### 14.1. 技術的成功基準

1. ✅ Acontextサーバーが正常に起動
2. ✅ beauty projectからAcontext APIに接続可能
3. ✅ セッション作成が正常に動作（成功率: 95%以上）
4. ✅ メッセージ保存が正常に動作（成功率: 95%以上）
5. ✅ タスク抽出が正常に動作（成功率: 80%以上）

### 14.2. 機能的な価値基準

1. ✅ 戦略分析実行時にセッションが自動作成される
2. ✅ 会話履歴がAcontextに保存される
3. ✅ Acontextダッシュボードでセッション・メッセージを確認できる
4. ✅ タスクが自動抽出され、進捗が追跡される

### 14.3. パフォーマンス基準

1. ✅ 既存機能の応答時間に影響がない（±10%以内）
2. ✅ Acontext統合による追加レイテンシが500ms以内
3. ✅ エラー時のフォールバックが500ms以内

### 14.4. 運用基準

1. ✅ エラーハンドリングが適切に実装されている
2. ✅ Acontextサーバー停止時も既存機能が動作する
3. ✅ エラーログが適切に記録される

---

## 15. 評価方法

### 15.1. PoC評価項目

**技術評価:**
- 実装の容易さ
- パフォーマンスへの影響
- エラーハンドリングの適切性

**機能評価:**
- セッション管理の有用性
- タスク追跡の有用性
- ダッシュボードの使いやすさ

**ビジネス評価:**
- ユーザー体験の向上
- 開発効率の向上
- 長期的な価値

### 15.2. 評価結果の活用

**次のステップの決定:**
- PoCが成功した場合: フェーズ2（スキル学習）の実装を検討
- PoCが部分的に成功した場合: 改善点を特定して再評価
- PoCが失敗した場合: 代替案を検討

---

## 16. 付録

### 16.1. 用語集

- **Session**: Acontextの会話スレッド。メッセージを保存するコンテナ。
- **Space**: Acontextのスキル保存領域。Notionライクな構造。
- **Task Agent**: Acontextのバックグラウンドエージェント。タスクを自動抽出。
- **Experience Agent**: Acontextのバックグラウンドエージェント。スキルを抽出・保存。
- **SOP**: Standard Operating Procedure。標準的な手順。

### 16.2. 参考資料

- [Acontext公式ドキュメント](https://docs.acontext.io/)
- [Acontext GitHub](https://github.com/memodb-io/Acontext)
- [Acontext統合分析レポート](./ACONTEXT_INTEGRATION_ANALYSIS.md)
- [Acontext統合実装例](./ACONTEXT_INTEGRATION_EXAMPLE.md)

### 16.3. 変更履歴

| 日付 | バージョン | 変更内容 | 変更者 |
|------|-----------|---------|--------|
| 2025-01-XX | 1.0 | 初版作成 | AI Assistant |

---

**承認:**

- **作成者**: AI Assistant
- **レビュー者**: [要記入]
- **承認者**: [要記入]
- **承認日**: [要記入]


